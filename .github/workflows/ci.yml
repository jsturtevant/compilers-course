name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    name: Build and Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Install Rust
      uses: dtolnay/rust-toolchain@stable
      with:
        components: rustfmt, clippy
    
    - name: Cache cargo dependencies
      uses: actions/cache@v3
      with:
        path: |
          ~/.cargo/bin/
          ~/.cargo/registry/index/
          ~/.cargo/registry/cache/
          ~/.cargo/git/db/
          target/
        key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
        restore-keys: |
          ${{ runner.os }}-cargo-
    
    - name: Check code formatting
      run: cargo fmt --all -- --check
    
    - name: Run clippy
      run: cargo clippy --all-targets --all-features -- -D warnings
    
    - name: Build all crates
      run: cargo build --verbose
    
    - name: Run unit tests
      run: cargo test --verbose

  lexer-tests:
    name: Lexer Tests (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    needs: build
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Install Rust
      uses: dtolnay/rust-toolchain@stable
    
    - name: Cache cargo dependencies
      uses: actions/cache@v3
      with:
        path: |
          ~/.cargo/bin/
          ~/.cargo/registry/index/
          ~/.cargo/registry/cache/
          ~/.cargo/git/db/
          target/
        key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
        restore-keys: |
          ${{ runner.os }}-cargo-
    
    - name: Run lexer on all samples (Unix)
      if: runner.os != 'Windows'
      run: ./scripts/run-lexer-all.sh
    
    - name: Run lexer on all samples (Windows)
      if: runner.os == 'Windows'
      run: cargo run -p lexer --quiet -- cool-support/examples/hello_world.cl
      shell: bash

  parser-tests:
    name: Parser Tests (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    needs: build
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Install Rust
      uses: dtolnay/rust-toolchain@stable
    
    - name: Cache cargo dependencies
      uses: actions/cache@v3
      with:
        path: |
          ~/.cargo/bin/
          ~/.cargo/registry/index/
          ~/.cargo/registry/cache/
          ~/.cargo/git/db/
          target/
        key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
        restore-keys: |
          ${{ runner.os }}-cargo-
    
    - name: Run parser on all samples (Unix)
      if: runner.os != 'Windows'
      run: ./scripts/run-parser-all.sh
    
    - name: Run parser on all samples (Windows)
      if: runner.os == 'Windows'
      run: cargo run -p parser --quiet -- cool-support/examples/hello_world.cl
      shell: bash

  semant-tests:
    name: Semant Tests (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    needs: build
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Install Rust
      uses: dtolnay/rust-toolchain@stable
    
    - name: Cache cargo dependencies
      uses: actions/cache@v3
      with:
        path: |
          ~/.cargo/bin/
          ~/.cargo/registry/index/
          ~/.cargo/registry/cache/
          ~/.cargo/git/db/
          target/
        key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
        restore-keys: |
          ${{ runner.os }}-cargo-
    
    - name: Run semantic analysis (Unix)
      if: runner.os != 'Windows'
      run: ./scripts/run-semant-all.sh
    
    - name: Run semantic analysis (Windows)
      if: runner.os == 'Windows'
      run: cargo run -p semant --quiet -- cool-support/examples/hello_world.cl
      shell: bash

  wasm-tests:
    name: WASM Tests (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    needs: build
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Install Rust
      uses: dtolnay/rust-toolchain@stable
    
    - name: Cache cargo dependencies
      uses: actions/cache@v3
      with:
        path: |
          ~/.cargo/bin/
          ~/.cargo/registry/index/
          ~/.cargo/registry/cache/
          ~/.cargo/git/db/
          target/
        key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
        restore-keys: |
          ${{ runner.os }}-cargo-
    
    - name: Install wasmtime (Unix)
      if: runner.os != 'Windows'
      run: |
        curl https://wasmtime.dev/install.sh -sSf | bash
        echo "$HOME/.wasmtime/bin" >> $GITHUB_PATH
    
    - name: Install wasmtime (Windows)
      if: runner.os == 'Windows'
      run: |
        iwr https://github.com/bytecodealliance/wasmtime/releases/download/v19.0.0/wasmtime-v19.0.0-x86_64-windows.zip -OutFile wasmtime.zip
        Expand-Archive wasmtime.zip -DestinationPath $env:USERPROFILE
        echo "$env:USERPROFILE\wasmtime-v19.0.0-x86_64-windows" >> $env:GITHUB_PATH
      shell: pwsh
    
    - name: Run WASM tests (Unix)
      if: runner.os != 'Windows'
      run: |
        ./scripts/run-codegen-all.sh
        ./scripts/run-wasmtime-all.sh
    
    - name: Run WASM tests (Windows)
      if: runner.os == 'Windows'
      run: |
        cargo run -p codegen --quiet -- cool-support/examples/hello_world.cl -o hello_world.wasm
        wasmtime hello_world.wasm
      shell: bash
